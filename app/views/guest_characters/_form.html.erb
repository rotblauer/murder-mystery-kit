<%= form_for(guest_character) do |f| %>
  <% if guest_character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(guest_character.errors.count, "error") %> prohibited this guest_character from being saved:</h2>

      <ul>
      <% guest_character.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :evening_id %>
    <%= f.text_field :evening_id %>
  </div>

  <div class="field">
    <%= f.label :guest_id %>
    <%= f.text_field :guest_id %>
  </div>

  <div class="field">
    <%= f.label :character_id %>
    <%= f.text_field :character_id %>
  </div>

  <div class="field">
    <%= f.label :index %>
    <%= f.text_field :index %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
