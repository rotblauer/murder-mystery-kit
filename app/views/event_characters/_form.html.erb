<%= form_for(event_character) do |f| %>
  <% if event_character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_character.errors.count, "error") %> prohibited this event_character from being saved:</h2>

      <ul>
      <% event_character.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %>
    <%= f.text_field :event_id %>
  </div>

  <div class="field">
    <%= f.label :character_id %>
    <%= f.text_field :character_id %>
  </div>

  <div class="field">
    <%= f.label :index %>
    <%= f.text_field :index %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
